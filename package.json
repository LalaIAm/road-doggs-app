{
  "name": "roaddoggs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "preview": "npm run preview --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "wait-on": "^9.0.3"
  }
}
